#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "src/vector.h"


#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))



int main(){

    double_vector_t v = {2};
    return 0;
}
